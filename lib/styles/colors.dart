import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFF5D4037);       // Brown
  static const Color primaryLight = Color(0xFFD7CCC8);  // Light brown
  static const Color primaryDark = Color(0xFF3E2723);   // Dark brown

  // Secondary / Accent Colors
  static const Color secondary = Color(0xFFFFA726);     // Orange
  static const Color secondaryLight = Color(0xFFFFD95B);
  static const Color secondaryDark = Color(0xFFEF6C00);

  // Success, Warning, Error Colors
  static const Color success = Color(0xFF4CAF50);        // Green
  static const Color warning = Color(0xFFFFC107);        // Amber
  static const Color error = Color(0xFFF44336);          // Red

  // Neutral / Grayscale
  static const Color black = Color(0xFF000000);
  static const Color white = Color(0xFFFFFFFF);
  static const Color gray = Color(0xFF9E9E9E);
  static const Color lightGray = Color(0xFFEEEEEE);
  static const Color darkGray = Color(0xFF424242);

  // Backgrounds
  static const Color background = Color(0xFFFFFBF2);     // Soft beige background
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color inputBackground = Color(0xFFF5F5F5);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = white;
  static const Color textOnSecondary = black;
}
